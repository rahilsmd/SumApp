apiVersion: extensions/v1beta1 
kind: Deployment
metadata:
  name: $appname
  labels:
    app: $appname
spec:
  selector:
    matchLabels:
      app: $appname
  replicas: 1
  template:
    metadata:
      labels:
        app: $appname
    spec:
      containers:
      - name: $APPNAME
        image: mycluster.icp:8500/default/$appname
        imagePullPolicy: Always
        ports:
        - containerPort: 9080
